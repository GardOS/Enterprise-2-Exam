bookServerPath: http://localhost:8099/books
userServerPath: http://localhost:8099/users

spring:
  session:
    store-type: none

eureka:
  client:
    enabled: false

logging:
  level:
    org.apache.http: debug #For printing http messages TODO: remove